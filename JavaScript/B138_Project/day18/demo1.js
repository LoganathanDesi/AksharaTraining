//JSON 
let emp={
    name:'Bhanu'
}

console.log(emp);
console.log(typeof emp);
//to access the attribute present inside the object we use access operator
console.log(emp.name);
console.log(emp['name']);
console.log(emp['id']);
