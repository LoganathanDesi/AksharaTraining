//Q6. Write a program to calculate the perimeter of a rectangle.
let length = 5;
let width = 10;
let perimeter = 2 * (length + width);
console.log("Perimeter of the rectangle:", perimeter);

//Q7. Write a program to calculate the volume of a sphere.
let radius = 7;
let volume = (4/3) * (22/7) * Math.pow(radius, 3);
console.log("Volume of the sphere:", volume);

//Q8. Write a program to calculate the surface area of a cylinder.
let radiusCyl = 8;
let height = 9;
let surfaceArea = 2 * Math.PI * radiusCyl * (radiusCyl + height);
console.log("Surface area of the cylinder:", surfaceArea);