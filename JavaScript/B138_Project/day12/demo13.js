let plus=(a,b)=>{return a+b};
let sum=plus(10,20);
console.log(sum);

let add=(a,b)=>a+b; // if the arrow function has just 1 return statement, then no need to use {} and return word
let res=add(10,20);
console.log(res);

